<div class="container">
    <div style="max-width: 600px; border: 2px solid darkgray" class="container mb-4">
        <h5 class="text-center mb-4"> {{info.title}}</h5>
        <p>{{info.text}}</p>
    </div>
</div>


<div style="max-width: 800px" class="container">
    <table  class="table table-hover">
        <thead>
        <tr>
            <th scope="col"></th>
            <th scope="col">Animal</th>
            <th scope="col">Size</th>
            <th scope="col">Price</th>
            <th scope="col">Stock</th>
        </tr>
        </thead>
        <tbody>
        {{#each items}}
            <tr style="cursor: pointer">
                <td></td>
                <td>{{this.name}} the {{this.species}}</td>
                <td>{{this.size}}</td>
                <td>{{this.price}}€</td>
                <td>{{this.numberInStock}}</td>
            </tr>
        {{/each}}
        </tbody>
    </table>
</div>

